[package]
name = "ironcore-alloy-streaming-roundtrip"
version = "0.1.0"
edition = "2021"

[dependencies]
ironcore-alloy = { path = "../../../../" }
tokio = { version = "1", features = [
    "rt-multi-thread",
    "time",
    "sync",
    "macros",
] }
